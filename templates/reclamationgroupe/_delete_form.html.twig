<form method="post" action="{{ path('app_reclamationgroupe_delete', {'idReclamation': reclamationgroupe.idReclamation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamationgroupe.idReclamation) }}">
    <button class="btn">Delete</button>
</form>
