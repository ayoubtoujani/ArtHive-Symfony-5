{% extends 'feed.html.twig' %}

{% block title %}Reclamationgroupe{% endblock %}

{% block content %}
    <h1>Reclamationgroupe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdReclamation</th>
                <td>{{ reclamationgroupe.idReclamation }}</td>
            </tr>
            <tr>
                <th>DescReclamation</th>
                <td>{{ reclamationgroupe.descReclamation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reclamationgroupe_index') }}">back to list</a>

    <a href="{{ path('app_reclamationgroupe_edit', {'idReclamation': reclamationgroupe.idReclamation}) }}">edit</a>

    {{ include('reclamationgroupe/_delete_form.html.twig') }}
{% endblock %}
